# Legacy Scripts

This directory contains ad-hoc validation and debugging scripts that were
previously stored in the repository root.

These scripts are not part of the standard test suite. Use `tests/` for active
automated checks.
