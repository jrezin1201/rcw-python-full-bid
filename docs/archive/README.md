# Archive Notes

This folder contains historical reports and migration notes that are useful for
reference but are not part of the active development workflow.

Active implementation docs should stay at the repository root or under `docs/`.
